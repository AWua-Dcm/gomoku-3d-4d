# 打开流程

**中文** | [English](README.en.md) —— 英文版是给玩家看的 README；这份流程文档只有中文。

工程位置：仓库根目录。下面 `Web_Gomoku3D/` 是网页版本体，`_verify/` 是验证脚本。

> **这是历史沿革，不是当前状态。** 这里原来是个 Unity 工程，Unity/C# 实现已经删除，
> 现在只有网页版。文档里凡是提到"C# 内核""C# 导出的向量"的地方，指的都是那段**已经不存在**
> 的历史 —— 那些向量是冻结的证据，不是活的代码。详见 `Web_Gomoku3D/README.md`。

---

## A. 玩

1. 打开文件夹 `Web_Gomoku3D\`
2. **双击 `index.html`**，用任意现代浏览器打开

不需要服务器、不需要联网、不需要装任何东西。

想确认没报错：按 `F12` → Console 标签，看有没有红色文字。

### 验收清单

| 检查 | 期望 |
|---|---|
| 起始界面 | 左侧一张缓慢自转的 10×10×10 演示棋盘（**压淡**，当背景用），右侧设置区 **和左侧是同一张纸、中间没有缝** |
| 选先手 / 尺寸 → 点「开始游戏」 | 进入对局 |
| 左侧按住左键拖拽 | 棋盘跟着转。往一个方向**一直拖到底**应该停在接近正俯视/正仰视处（不会突然滚半圈，也不会在极点附近抖动） |
| 滚轮 | 缩放 |
| 松手不动 | 棋盘**完全静止**（没有惯性是刻意的） |
| 左侧悬停 | 红线标出落点的 x / y / z 三条贯穿线 |
| 右侧面板点一个空格子 | **左侧同一位置立刻出现一颗子** ← 重点看这条 |
| 左上「格线」/ 按 `H` | 三维棋盘只剩棋子。**蓝框、红线、金线都还在**；盘里的子应该明显比关之前好认（对照 15³ 那一片灰网） |
| 下满 5 连 | 中间弹出金色横幅 |
| 拖动横幅 / 点「关闭 Esc」/ 按 Esc | 横幅跟着手走、拖不出左侧视图；能收起。**收起后点棋盘仍然落不下子，并且会提示「本局已结束」**；点「悔棋」之后才能接着下这一局 |
| `Z` / `R` / `G` / `H` | 悔棋 / 重开 / 切换视图模式 / 隐藏显示格线 |
| `Q` `E` / `[` `]` | 切换当前层 |
| 右上角「具体规则」 | 弹出规则全文浮层，Esc 关闭。**浮层开着时其他快捷键失效**（刻意的） |
| 四维模式：右侧「转动一层」面板 | 选轴 / 层 / 方向 / 次数 → 「执行转动」（或按 `T`），绿框标出待转的那一层 |

完整的操作表见 [Web_Gomoku3D/README.md](Web_Gomoku3D/README.md)。

---

## B. 跑测试

```bash
bash _verify/run-all.sh
```

八步（第 8 步可选）。**不需要 Unity，也不需要 .NET，只要有 node。**

期望结果：11733 + 19408 + 98 + 77 + 8845 + 115 + 124 = **40400 项断言全绿**。

单跑某一项：

| 命令 | 项数 |
|---|---|
| `node Web_Gomoku3D/tests/rules.test.mjs` | 11733（三维规则基线） |
| `node Web_Gomoku3D/tests/rotation.test.mjs` | 19408（四维转动一致性） |
| `node Web_Gomoku3D/tests/dims.test.mjs` | 98（长方体 + 尺寸约束） |
| `node Web_Gomoku3D/tests/dom-smoke.test.mjs` | 93（界面桩环境，含六语言文案齐全性） |
| `node Web_Gomoku3D/tests/online.test.mjs` | 8845（联机内核与网页版逐格对拍） |
| `node Web_Gomoku3D/tests/online-http.test.mjs` | 115（联机 HTTP 层） |
| `node _verify/browser-check.mjs` | 124（真实无头浏览器；没装浏览器会自动跳过） |

第 8 步的截图落在 `_verify/shots/`。

> ⚠️ **`tests/vectors.json` 和 `tests/rotation-vectors.json` 是冻结的证据。**
> 导出它们的 C# 内核已经删除，这两个文件**无法重新生成**，`run-all.sh` 第 2 步会用 md5 钉死。
> 不要为了让测试变绿而改它们 —— 那不是修 bug，是篡改证据。详见 `_verify/run-all.sh` 文件头的说明。

---

## 一个已知的坑

**路径里的空格。** 用命令行给脚本传路径时**永远加双引号** —— 一旦路径里有空格，
shell 会把它拆成多个参数，然后拼出一个不存在的路径并**静默失败**（返回码 0，什么都不显示）。
这个失败方式特别难查：命令看起来跑了，什么都没输出，但你以为它成功了。

`run-all.sh` 和各个测试脚本内部都用了引号 / `path.join`，不会踩到。

---

## 还没验证过的部分（需要你确认）

| 项 | 怎么看 |
|---|---|
| 配色好不好看、三维观感 | A 节逐条看 |
| 交互手感（拖拽阈值、拾取精度、缩放速率） | A 节 |
| 四维转动的可读性（绿框在密集格网里会不会看糊、转完"唰"地一下的手感） | A 节开一局四维 |
| 比 900×700 更窄的窗口 | browser-check 已经自动验过 900×700（无横向滚动、切模式时按钮不动），更窄的还得你拖窄了看 |
| 英文文案读起来地不地道、英文排版好不好看 | 点右上角切到 `EN`，对着 `_verify/shots/6-起始界面-英文.png` 那张看 |
| 英文的术语是否前后一致 | 同上；术语表在 `Web_Gomoku3D/README.md` |

其余部分（40400 项断言）都已经真实跑过并通过。
